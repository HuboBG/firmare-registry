# Firmware Registry UI (Vue 3 + Vite)

Admin UI for Firmware Registry API.

- Uses API only (no DB coupling)
- Reads admin/device keys from .env
- Suitable behind nginx BasicAuth

## Dev
npm install
npm run dev

## Env
See .env.example
